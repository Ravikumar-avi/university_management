<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View - Lab Schedule -->
    <record id="lab_schedule_view_form" model="ir.ui.view">
        <field name="name">lab.schedule.view.form</field>
        <field name="model">lab.schedule</field>
        <field name="arch" type="xml">
            <form string="Laboratory Schedule">
                <header>
                    <field name="state" widget="statusbar"
                           statusbar_visible="available,maintenance,closed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Lab Name"/>
                        <h1>
                            <field name="name" placeholder="e.g., Computer Lab 1" required="1"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Lab Details">
                            <field name="lab_number" placeholder="e.g., LAB-CS-01"/>
                            <field name="lab_type"/>
                            <field name="capacity" placeholder="Number of students"/>
                            <field name="department_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                        </group>
                        <group string="Lab Management">
                            <field name="lab_incharge_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Lab Schedule" name="schedule">
                            <field name="timetable_ids"
                                   context="{'default_class_type': 'practical', 'default_room_number': lab_number}">
                                <list>
                                    <field name="day_of_week"/>
                                    <field name="start_time" widget="float_time"/>
                                    <field name="end_time" widget="float_time"/>
                                    <field name="subject_id"/>
                                    <field name="faculty_id"/>
                                    <field name="batch_id"/>
                                    <field name="section" optional="show"/>
                                    <field name="active" optional="show"/>
                                </list>
                            </field>
                        </page>

                        <page string="Equipment Details" name="equipment">
                            <field name="equipment_details"
                                   placeholder="List all equipment, apparatus, tools, and resources available in this lab..."/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View - Lab Schedule -->
    <record id="lab_schedule_view_list" model="ir.ui.view">
        <field name="name">lab.schedule.view.list</field>
        <field name="model">lab.schedule</field>
        <field name="arch" type="xml">
            <list string="Laboratory Schedule"
                  multi_edit="1"
                  sample="1"
                  decoration-success="state == 'available'"
                  decoration-warning="state == 'maintenance'"
                  decoration-danger="state == 'closed'"
                  decoration-muted="active == False">
                <field name="name"/>
                <field name="lab_number"/>
                <field name="lab_type" optional="show"/>
                <field name="capacity" optional="show"/>
                <field name="department_id" optional="show"/>
                <field name="lab_incharge_id" optional="show"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'available'"
                       decoration-warning="state == 'maintenance'"
                       decoration-danger="state == 'closed'"/>
                <field name="active" optional="show" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Search View - Lab Schedule -->
    <record id="lab_schedule_view_search" model="ir.ui.view">
        <field name="name">lab.schedule.view.search</field>
        <field name="model">lab.schedule</field>
        <field name="arch" type="xml">
            <search string="Search Labs">
                <field name="name"/>
                <field name="lab_number"/>
                <field name="department_id"/>
                <field name="lab_incharge_id"/>

                <separator/>

                <filter name="filter_active" string="Active"
                        domain="[('active', '=', True)]"/>
                <filter name="filter_inactive" string="Inactive"
                        domain="[('active', '=', False)]"/>

                <separator/>

                <filter name="filter_available" string="Available"
                        domain="[('state', '=', 'available')]"/>
                <filter name="filter_maintenance" string="Under Maintenance"
                        domain="[('state', '=', 'maintenance')]"/>
                <filter name="filter_closed" string="Closed"
                        domain="[('state', '=', 'closed')]"/>

                <separator/>

                <filter name="filter_computer" string="Computer Lab"
                        domain="[('lab_type', '=', 'computer')]"/>
                <filter name="filter_physics" string="Physics Lab"
                        domain="[('lab_type', '=', 'physics')]"/>
                <filter name="filter_chemistry" string="Chemistry Lab"
                        domain="[('lab_type', '=', 'chemistry')]"/>
                <filter name="filter_electronics" string="Electronics Lab"
                        domain="[('lab_type', '=', 'electronics')]"/>
                <filter name="filter_mechanical" string="Mechanical Workshop"
                        domain="[('lab_type', '=', 'mechanical')]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_lab_type" string="Lab Type"
                            context="{'group_by': 'lab_type'}"/>
                    <filter name="group_by_department" string="Department"
                            context="{'group_by': 'department_id'}"/>
                    <filter name="group_by_incharge" string="Lab In-charge"
                            context="{'group_by': 'lab_incharge_id'}"/>
                    <filter name="group_by_state" string="Status"
                            context="{'group_by': 'state'}"/>
                </group>

                <searchpanel>
                    <field name="lab_type" string="Lab Type"
                           icon="fa-flask"/>
                    <field name="department_id" string="Department"
                           icon="fa-building"/>
                    <field name="state" string="Status"
                           select="multi"
                           icon="fa-flag"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Lab Schedule -->
    <record id="lab_schedule_view_kanban" model="ir.ui.view">
        <field name="name">lab.schedule.view.kanban</field>
        <field name="model">lab.schedule</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="lab_type"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="lab_number"/>
                <field name="lab_type"/>
                <field name="capacity"/>
                <field name="department_id"/>
                <field name="lab_incharge_id"/>
                <field name="state"/>
                <field name="active"/>
                <field name="activity_ids"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click"
                             t-attf-class="{{!record.active.raw_value ? 'opacity-50' : ''}}">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="name"/>
                                        </strong>
                                        <div class="text-muted mt-1">
                                            <small><field name="lab_number"/></small>
                                        </div>
                                    </div>
                                    <div class="o_kanban_record_headings text-end">
                                        <span t-attf-class="badge {{
                                            record.state.raw_value == 'available' ? 'bg-success' :
                                            (record.state.raw_value == 'maintenance' ? 'bg-warning' : 'bg-danger')
                                        }}">
                                            <field name="state"/>
                                        </span>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2" t-if="record.capacity.raw_value">
                                        <div class="col-12">
                                            <div class="alert alert-info mb-0 p-2 text-center">
                                                <h5 class="mb-0">
                                                    <i class="fa fa-users"/>
                                                    Capacity: <field name="capacity"/> Students
                                                </h5>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.department_id.raw_value">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted d-block">Department</small>
                                                <small>
                                                    <i class="fa fa-building text-primary"/>
                                                    <field name="department_id"/>
                                                </small>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.lab_incharge_id.raw_value">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted d-block">Lab In-charge</small>
                                                <small>
                                                    <i class="fa fa-user text-success"/>
                                                    <field name="lab_incharge_id"/>
                                                </small>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row" t-if="!record.active.raw_value">
                                        <div class="col-12">
                                            <span class="badge bg-secondary">
                                                <i class="fa fa-ban"/> Inactive
                                            </span>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Action -->
    <record id="action_lab_schedule" model="ir.actions.act_window">
        <field name="name">Lab Schedule</field>
        <field name="res_model">lab.schedule</field>
        <field name="view_mode">kanban,list,form</field>
        <field name="context">{'search_default_filter_active': 1, 'search_default_filter_available': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Laboratory Schedule
            </p>
            <p>
                Manage laboratory schedules with comprehensive features. Enter name (char required, "Lab Name",
                h1 title labeled "Lab Name", placeholder "e.g., Computer Lab 1"). Enter lab_number (char
                required, "Lab Number", placeholder "e.g., LAB-CS-01"). Select lab_type (6 options, "Lab Type"):
                computer (Computer Lab), physics (Physics Lab), chemistry (Chemistry Lab), electronics
                (Electronics Lab), mechanical (Mechanical Workshop), other (Other). Enter capacity (integer,
                "Capacity", placeholder "Number of students"). Select department_id (Many2one to
                university.department, "Department", no-create). Select lab_incharge_id (Many2one to
                faculty.faculty, "Lab In-charge", no-create). View timetable_ids (One2many to class.timetable
                filtered by room_number with domain [('class_type','=','practical')], "Lab Schedule", list view
                showing day_of_week/start_time float_time/end_time float_time/subject_id/faculty_id/batch_id/
                section optional/active optional, context default_class_type practical/default_room_number
                lab_number). Enter equipment_details (html, "Equipment/Apparatus Details", placeholder "List all
                equipment, apparatus, tools, and resources available in this lab..."). Select state (3 options
                default available tracked, "Status", statusbar visible available-maintenance-closed): available
                (Available), maintenance (Under Maintenance), closed (Closed). Check active (boolean default
                True, boolean_toggle widget). Form view with header statusbar for state, 2 groups: Lab Details
                (lab_number/lab_type/capacity/department_id no-create) and Lab Management (lab_incharge_id
                no-create/active toggle), notebook with 2 pages: Lab Schedule (timetable_ids One2many list with
                context defaults) and Equipment Details (equipment_details html with placeholder). List view
                with multi_edit, 4-color row decorations (success for state available, warning for maintenance,
                danger for closed, muted for inactive), badge widget for state with 3 decorations (success-
                warning-danger), optional fields, boolean_toggle for active. Kanban view grouped by lab_type
                showing name as title with lab_number muted small text, state badge at top-right (3-color:
                success green for available, warning yellow for maintenance, danger red for closed), conditional
                capacity alert (info background, users icon, "Capacity: X Students" h5 text, shown if capacity
                exists), conditional department_id card (light background, building icon primary, "Department"
                label, shown if department_id exists), conditional lab_incharge_id card (light background, user
                icon success, "Lab In-charge" label, shown if lab_incharge_id exists), conditional inactive
                badge (secondary background, ban icon, shown if not active), opacity-50 class for entire card if
                inactive, activity widget at bottom-right. Ordered by day_of_week and start_time (inherited from
                _order). Default filters: Active and Available. Comprehensive search filters for active-inactive/
                state 3 options/lab_type 5 options/group by lab_type-department-incharge-state. Searchpanel for
                lab_type, department_id, and state multi-select. Full chatter/activity support. Complete lab
                management with timetable integration filtered for practical classes only.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_lab_schedule"-->
<!--              name="Lab Schedule"-->
<!--              parent="menu_timetable_root"-->
<!--              action="action_lab_schedule"-->
<!--              sequence="30"/>-->

</odoo>
