<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View - Faculty Timetable -->
    <record id="faculty_timetable_view_list" model="ir.ui.view">
        <field name="name">faculty.timetable.view.list</field>
        <field name="model">faculty.timetable</field>
        <field name="arch" type="xml">
            <list string="Faculty Timetable"
                  create="false"
                  edit="false"
                  delete="false"
                  default_order="faculty_id, day_of_week, start_time"
                  sample="1">
                <field name="faculty_id"/>
                <field name="day_of_week"/>
                <field name="start_time" widget="float_time"/>
                <field name="end_time" widget="float_time"/>
                <field name="subject_id"/>
                <field name="batch_id"/>
                <field name="room_number" optional="show"/>
            </list>
        </field>
    </record>

    <!-- Search View - Faculty Timetable -->
    <record id="faculty_timetable_view_search" model="ir.ui.view">
        <field name="name">faculty.timetable.view.search</field>
        <field name="model">faculty.timetable</field>
        <field name="arch" type="xml">
            <search string="Search Faculty Timetable">
                <field name="faculty_id"/>
                <field name="subject_id"/>
                <field name="batch_id"/>
                <field name="room_number"/>

                <separator/>

                <filter name="filter_monday" string="Monday"
                        domain="[('day_of_week', '=', 'monday')]"/>
                <filter name="filter_tuesday" string="Tuesday"
                        domain="[('day_of_week', '=', 'tuesday')]"/>
                <filter name="filter_wednesday" string="Wednesday"
                        domain="[('day_of_week', '=', 'wednesday')]"/>
                <filter name="filter_thursday" string="Thursday"
                        domain="[('day_of_week', '=', 'thursday')]"/>
                <filter name="filter_friday" string="Friday"
                        domain="[('day_of_week', '=', 'friday')]"/>
                <filter name="filter_saturday" string="Saturday"
                        domain="[('day_of_week', '=', 'saturday')]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_faculty" string="Faculty"
                            context="{'group_by': 'faculty_id'}"/>
                    <filter name="group_by_day" string="Day"
                            context="{'group_by': 'day_of_week'}"/>
                    <filter name="group_by_subject" string="Subject"
                            context="{'group_by': 'subject_id'}"/>
                    <filter name="group_by_batch" string="Batch"
                            context="{'group_by': 'batch_id'}"/>
                </group>

                <searchpanel>
                    <field name="faculty_id" string="Faculty"
                           icon="fa-user"/>
                    <field name="day_of_week" string="Day"
                           select="multi"
                           icon="fa-calendar-o"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Faculty Timetable -->
    <record id="faculty_timetable_view_kanban" model="ir.ui.view">
        <field name="name">faculty.timetable.view.kanban</field>
        <field name="model">faculty.timetable</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="faculty_id"
                    create="false"
                    edit="false"
                    delete="false"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="faculty_id"/>
                <field name="day_of_week"/>
                <field name="start_time"/>
                <field name="end_time"/>
                <field name="subject_id"/>
                <field name="batch_id"/>
                <field name="room_number"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="subject_id"/>
                                        </strong>
                                        <div class="text-muted mt-1">
                                            <small>
                                                <span class="badge bg-primary">
                                                    <field name="day_of_week"/>
                                                </span>
                                            </small>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="alert alert-info mb-0 p-2 text-center">
                                                <h5 class="mb-0">
                                                    <i class="fa fa-clock-o"/>
                                                    <field name="start_time" widget="float_time"/> -
                                                    <field name="end_time" widget="float_time"/>
                                                </h5>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted d-block">Batch</small>
                                                <small>
                                                    <i class="fa fa-users text-success"/>
                                                    <field name="batch_id"/>
                                                </small>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row" t-if="record.room_number.raw_value">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted d-block">Room</small>
                                                <small>
                                                    <i class="fa fa-map-marker text-danger"/>
                                                    <field name="room_number"/>
                                                </small>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View - Faculty Timetable -->
    <record id="faculty_timetable_view_calendar" model="ir.ui.view">
        <field name="name">faculty.timetable.view.calendar</field>
        <field name="model">faculty.timetable</field>
        <field name="arch" type="xml">
            <calendar string="Faculty Schedule Calendar"
                      date_start="start_time"
                      date_stop="end_time"
                      mode="week"
                      quick_create="0"
                      create="0"
                      edit="0"
                      delete="0"
                      color="faculty_id"
                      event_open_popup="1"
                      hide_time="0"
                      scales="week">
                <field name="subject_id"/>
                <field name="faculty_id"/>
                <field name="batch_id"/>
                <field name="room_number"/>
                <field name="day_of_week" filters="1" invisible="1"/>
            </calendar>
        </field>
    </record>

    <!-- Pivot View - Faculty Timetable -->
    <record id="faculty_timetable_view_pivot" model="ir.ui.view">
        <field name="name">faculty.timetable.view.pivot</field>
        <field name="model">faculty.timetable</field>
        <field name="arch" type="xml">
            <pivot string="Faculty Schedule Analysis">
                <field name="faculty_id" type="row"/>
                <field name="day_of_week" type="col"/>
                <field name="duration" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- Graph View - Faculty Timetable -->
    <record id="faculty_timetable_view_graph" model="ir.ui.view">
        <field name="name">faculty.timetable.view.graph</field>
        <field name="model">faculty.timetable</field>
        <field name="arch" type="xml">
            <graph string="Faculty Workload Analysis"
                   type="bar"
                   sample="1">
                <field name="faculty_id"/>
                <field name="duration" type="measure"/>
            </graph>
        </field>
    </record>

    <!-- Action -->
    <record id="action_faculty_timetable" model="ir.actions.act_window">
        <field name="name">Faculty Timetable</field>
        <field name="res_model">faculty.timetable</field>
        <field name="view_mode">calendar,list,kanban,pivot,graph</field>
        <field name="context">{'search_default_group_by_faculty': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Faculty Timetable View
            </p>
            <p>
                View faculty teaching schedules in a read-only format. This is a SQL VIEW that automatically
                aggregates data from class timetable entries. View faculty_id (Many2one to faculty.faculty
                readonly), 6 day_of_week options readonly (monday/Monday, tuesday/Tuesday, wednesday/Wednesday,
                thursday/Thursday, friday/Friday, saturday/Saturday), start_time (float readonly, float_time
                widget), end_time (float readonly, float_time widget), subject_id (Many2one to university.subject
                readonly), batch_id (Many2one to university.batch readonly), room_number (char readonly). List
                view with create/edit/delete disabled (false), ordered by faculty_id/day_of_week/start_time,
                float_time widgets for start_time/end_time, optional room_number. Kanban view grouped by
                faculty_id showing subject_id as title with day_of_week badge (primary background), centered
                time alert (info background, clock icon, h5 start_time-end_time float_time widgets), batch_id
                card (light background, users icon success, "Batch" label), conditional room_number card (light
                background, map-marker icon danger, "Room" label, shown if room_number exists),
                create/edit/delete disabled. Calendar view with date_start/date_stop using start_time/end_time,
                mode week, color by faculty_id, day_of_week filters, event_open_popup, scales week only,
                create/edit/delete disabled. Pivot view with faculty_id as row, day_of_week as col, subject_id
                as measure, create/edit/delete disabled. Graph view (bar chart) showing faculty_id vs
                day_of_week measure for workload analysis, create disabled. Comprehensive search filters for 6
                days (monday-saturday)/group by faculty-day-subject-batch. Searchpanel for faculty_id and
                day_of_week multi-select. Default filter: grouped by Faculty. SQL VIEW created in init() method
                using tools.drop_view_if_exists and CREATE OR REPLACE VIEW selecting ROW_NUMBER() OVER() as id,
                faculty_id, day_of_week, start_time, end_time, subject_id, batch_id, room_number FROM
                class_timetable WHERE active=true. Complete read-only faculty schedule view for teaching load
                analysis and schedule management.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_faculty_timetable"-->
<!--              name="Faculty Timetable"-->
<!--              parent="menu_timetable_root"-->
<!--              action="action_faculty_timetable"-->
<!--              sequence="20"/>-->

</odoo>
