<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View - Class Timetable -->
    <record id="class_timetable_view_form" model="ir.ui.view">
        <field name="name">class.timetable.view.form</field>
        <field name="model">class.timetable</field>
        <field name="arch" type="xml">
            <form string="Class Timetable">
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Class Schedule"/>
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Academic Information">
                            <field name="academic_year_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="semester_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="program_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="department_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="batch_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="section" placeholder="e.g., A, B, C"/>
                        </group>
                        <group string="Schedule">
                            <field name="day_of_week"/>
                            <field name="start_time" widget="float_time"
                                   placeholder="e.g., 9.0 for 9:00 AM"/>
                            <field name="end_time" widget="float_time"
                                   placeholder="e.g., 10.0 for 10:00 AM"/>
                            <field name="duration" readonly="1" widget="float_time"/>
                        </group>
                    </group>

                    <group>
                        <group string="Subject Details">
                            <field name="course_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="subject_id" readonly="1"/>
                            <field name="class_type"/>
                        </group>
                        <group string="Faculty &amp; Venue">
                            <field name="faculty_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="room_number"
                                   placeholder="e.g., Room 301, Lab 1"/>
                            <field name="building"
                                   placeholder="e.g., Main Block, Engineering Block"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View - Class Timetable -->
    <record id="class_timetable_view_list" model="ir.ui.view">
        <field name="name">class.timetable.view.list</field>
        <field name="model">class.timetable</field>
        <field name="arch" type="xml">
            <list string="Class Timetable"
                  default_order="day_of_week, start_time"
                  multi_edit="1"
                  sample="1"
                  decoration-muted="active == False">
                <field name="day_of_week"/>
                <field name="start_time" widget="float_time"/>
                <field name="end_time" widget="float_time"/>
                <field name="duration" widget="float_time" optional="show"/>
                <field name="subject_id"/>
                <field name="class_type" optional="show"/>
                <field name="faculty_id"/>
                <field name="room_number" optional="show"/>
                <field name="building" optional="hide"/>
                <field name="batch_id" optional="show"/>
                <field name="section" optional="hide"/>
                <field name="semester_id" optional="hide"/>
                <field name="academic_year_id" optional="hide"/>
                <field name="active" optional="show" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Search View - Class Timetable -->
    <record id="class_timetable_view_search" model="ir.ui.view">
        <field name="name">class.timetable.view.search</field>
        <field name="model">class.timetable</field>
        <field name="arch" type="xml">
            <search string="Search Timetable">
                <field name="subject_id"/>
                <field name="faculty_id"/>
                <field name="batch_id"/>
                <field name="department_id"/>
                <field name="room_number"/>

                <separator/>

                <filter name="filter_active" string="Active"
                        domain="[('active', '=', True)]"/>
                <filter name="filter_inactive" string="Inactive"
                        domain="[('active', '=', False)]"/>

                <separator/>

                <filter name="filter_theory" string="Theory Classes"
                        domain="[('class_type', '=', 'theory')]"/>
                <filter name="filter_practical" string="Practical/Lab"
                        domain="[('class_type', '=', 'practical')]"/>
                <filter name="filter_tutorial" string="Tutorials"
                        domain="[('class_type', '=', 'tutorial')]"/>

                <separator/>

                <filter name="filter_monday" string="Monday"
                        domain="[('day_of_week', '=', 'monday')]"/>
                <filter name="filter_tuesday" string="Tuesday"
                        domain="[('day_of_week', '=', 'tuesday')]"/>
                <filter name="filter_wednesday" string="Wednesday"
                        domain="[('day_of_week', '=', 'wednesday')]"/>
                <filter name="filter_thursday" string="Thursday"
                        domain="[('day_of_week', '=', 'thursday')]"/>
                <filter name="filter_friday" string="Friday"
                        domain="[('day_of_week', '=', 'friday')]"/>
                <filter name="filter_saturday" string="Saturday"
                        domain="[('day_of_week', '=', 'saturday')]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_day" string="Day"
                            context="{'group_by': 'day_of_week'}"/>
                    <filter name="group_by_batch" string="Batch/Year"
                            context="{'group_by': 'batch_id'}"/>
                    <filter name="group_by_department" string="Department"
                            context="{'group_by': 'department_id'}"/>
                    <filter name="group_by_faculty" string="Faculty"
                            context="{'group_by': 'faculty_id'}"/>
                    <filter name="group_by_class_type" string="Class Type"
                            context="{'group_by': 'class_type'}"/>
                    <filter name="group_by_semester" string="Semester"
                            context="{'group_by': 'semester_id'}"/>
                    <filter name="group_by_academic_year" string="Academic Year"
                            context="{'group_by': 'academic_year_id'}"/>
                </group>

                <searchpanel>
                    <field name="academic_year_id" string="Academic Year"
                           icon="fa-calendar"/>
                    <field name="semester_id" string="Semester"
                           icon="fa-bookmark"/>
                    <field name="department_id" string="Department"
                           icon="fa-building"/>
                    <field name="day_of_week" string="Day"
                           select="multi"
                           icon="fa-calendar-o"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Class Timetable -->
    <record id="class_timetable_view_kanban" model="ir.ui.view">
        <field name="name">class.timetable.view.kanban</field>
        <field name="model">class.timetable</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="day_of_week"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="day_of_week"/>
                <field name="start_time"/>
                <field name="end_time"/>
                <field name="duration"/>
                <field name="subject_id"/>
                <field name="class_type"/>
                <field name="faculty_id"/>
                <field name="room_number"/>
                <field name="building"/>
                <field name="batch_id"/>
                <field name="section"/>
                <field name="active"/>
                <field name="activity_ids"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click"
                             t-attf-class="{{!record.active.raw_value ? 'opacity-50' : ''}}">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="subject_id"/>
                                        </strong>
                                        <div class="text-muted mt-1">
                                            <small>
                                                <span t-attf-class="badge {{
                                                    record.class_type.raw_value == 'theory' ? 'bg-primary' :
                                                    (record.class_type.raw_value == 'practical' ? 'bg-success' : 'bg-info')
                                                }}">
                                                    <field name="class_type"/>
                                                </span>
                                            </small>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="alert alert-primary mb-0 p-2 text-center">
                                                <h5 class="mb-0">
                                                    <i class="fa fa-clock-o"/>
                                                    <field name="start_time" widget="float_time"/> -
                                                    <field name="end_time" widget="float_time"/>
                                                </h5>
                                                <small class="text-muted">
                                                    Duration: <field name="duration" widget="float_time"/>
                                                </small>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted">Faculty</small>
                                                <h6 class="mb-0">
                                                    <i class="fa fa-user text-primary"/> <field name="faculty_id"/>
                                                </h6>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.room_number.raw_value">
                                        <div class="col-6">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted d-block">Room</small>
                                                <small>
                                                    <i class="fa fa-map-marker text-danger"/>
                                                    <field name="room_number"/>
                                                </small>
                                            </div>
                                        </div>
                                        <div class="col-6" t-if="record.building.raw_value">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted d-block">Building</small>
                                                <small>
                                                    <i class="fa fa-building text-info"/>
                                                    <field name="building"/>
                                                </small>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <small class="text-muted d-block">Batch/Section</small>
                                                <small>
                                                    <i class="fa fa-users text-success"/>
                                                    <field name="batch_id"/>
                                                    <span t-if="record.section.raw_value">
                                                        - Section <field name="section"/>
                                                    </span>
                                                </small>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row" t-if="!record.active.raw_value">
                                        <div class="col-12">
                                            <span class="badge bg-secondary">
                                                <i class="fa fa-ban"/> Inactive
                                            </span>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View - Class Timetable -->
    <record id="class_timetable_view_calendar" model="ir.ui.view">
        <field name="name">class.timetable.view.calendar</field>
        <field name="model">class.timetable</field>
        <field name="arch" type="xml">
            <calendar string="Timetable Calendar"
                      date_start="start_time"
                      date_stop="end_time"
                      mode="week"
                      quick_create="0"
                      color="faculty_id"
                      event_open_popup="1"
                      hide_time="0"
                      scales="week">
                <field name="subject_id"/>
                <field name="faculty_id"/>
                <field name="room_number"/>
                <field name="day_of_week" filters="1" invisible="1"/>
            </calendar>
        </field>
    </record>

    <!-- Action -->
    <record id="action_class_timetable" model="ir.actions.act_window">
        <field name="name">Class Timetable</field>
        <field name="res_model">class.timetable</field>
        <field name="view_mode">calendar,list,kanban,form</field>
        <field name="context">{'search_default_filter_active': 1, 'search_default_group_by_day': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Class Timetable Entry
            </p>
            <p>
                Manage class timetable/schedule with comprehensive features. View name (char computed stored
                readonly via _compute_name with api.depends('subject_id','day_of_week','start_time') formatted
                as "{subject_id.name} - {day_of_week} - {start_time}", displayed as h1 title labeled "Class
                Schedule"). Select academic_year_id (Many2one to university.academic.year required indexed,
                no-create). Select semester_id (Many2one to university.semester required indexed, no-create).
                Select program_id (Many2one to university.program required, no-create). Select department_id
                (Many2one to university.department required, no-create). Select batch_id (Many2one to
                university.batch required, no-create). Enter section (char, placeholder "e.g., A, B, C").
                Select day_of_week (6 options required indexed): monday (Monday), tuesday (Tuesday), wednesday
                (Wednesday), thursday (Thursday), friday (Friday), saturday (Saturday). Enter start_time (float
                required, float_time widget, placeholder "e.g., 9.0 for 9:00 AM", help text "Time in 24-hour
                format (e.g., 9.0 for 9:00 AM)"). Enter end_time (float required, float_time widget,
                placeholder "e.g., 10.0 for 10:00 AM"). View duration (float computed stored readonly via
                _compute_duration with api.depends('start_time','end_time') calculating end_time-start_time,
                float_time widget). Select course_id (Many2one to university.course required, no-create). View
                subject_id (Many2one related from course_id.subject_id stored readonly). Select class_type (3
                options required default theory): theory (Theory Class), practical (Practical/Lab), tutorial
                (Tutorial). Select faculty_id (Many2one to faculty.faculty required tracked, no-create). Enter
                room_number (char, placeholder "e.g., Room 301, Lab 1"). Enter building (char, placeholder
                "e.g., Main Block, Engineering Block"). Check active (boolean default True, boolean_toggle
                widget). Form view with 2 groups: Academic Information (academic_year_id/semester_id/
                program_id/department_id/batch_id/section) and Schedule (day_of_week/start_time float_time/
                end_time float_time/duration readonly float_time), 2 more groups: Subject Details (course_id/
                subject_id readonly/class_type) and Faculty &amp; Venue (faculty_id/room_number/building/
                active toggle). List view with float_time widgets for start_time/end_time/duration, muted row
                decoration for inactive records, optional field controls. Kanban view grouped by day_of_week
                showing subject_id as title with class_type badge (3-color: primary/blue for theory, success/
                green for practical, info/light-blue for tutorial), centered time alert (primary background,
                clock icon, h5 start_time-end_time float_time widgets, small "Duration: duration" text),
                faculty_id card (light background, user icon primary), conditional 2-card room_number/building
                (6/6 grid if room_number exists, map-marker icon danger/building icon info), batch_id/section
                card (light background, users icon success, "Batch/Section" label with conditional section),
                conditional inactive badge (secondary background, ban icon, shown if not active), opacity-50
                class for entire card if inactive, activity widget at bottom-right. Calendar view with
                date_start/date_stop start_time/end_time, mode week, color by faculty_id, day_of_week filters,
                event_open_popup, scales week only. Ordered by day_of_week and start_time (day then time
                order). Default filters: Active and grouped by Day. Comprehensive search filters for active-
                inactive/3 class types/6 days/group by day-batch-department-faculty-class_type-semester-
                academic_year. Searchpanel for academic_year_id, semester_id, department_id, and day_of_week
                multi-select. Full chatter/activity support. SQL constraint for unique slot (batch_id/
                day_of_week/start_time/room_number preventing same batch/room at same time). Time validation
                constraint via _check_times with api.constrains('start_time','end_time') checking end_time &gt;
                start_time and 0 &lt;= times &lt;= 24, raising ValidationError. Faculty conflict validation via
                _check_faculty_conflict with api.constrains('faculty_id','day_of_week','start_time',
                'end_time') searching for overlapping time slots (start_time &lt; other.end_time and end_time &gt;
                other.start_time) for same faculty/day, raising ValidationError with faculty name. Complete
                class scheduling with conflict detection and time validation.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_class_timetable"-->
<!--              name="Class Timetable"-->
<!--              parent="menu_timetable_root"-->
<!--              action="action_class_timetable"-->
<!--              sequence="10"/>-->

</odoo>
