<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View - Hostel Visitor -->
    <record id="hostel_visitor_view_form" model="ir.ui.view">
        <field name="name">hostel.visitor.view.form</field>
        <field name="model">hostel.visitor</field>
        <field name="arch" type="xml">
            <form string="Hostel Visitor">
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Visitor Name"/>
                        <h1>
                            <field name="name" placeholder="Enter visitor's full name..."/>
                        </h1>
                    </div>

                    <group>
                        <group string="Visitor Information">
                            <field name="visitor_type"/>
                            <field name="phone" placeholder="e.g., +91 9876543210"/>
                            <field name="id_proof_type"/>
                            <field name="id_proof_number" placeholder="Enter ID proof number..."/>
                        </group>
                        <group string="Approval">
                            <field name="approved_by" widget="many2one_avatar_user"/>
                        </group>
                    </group>

                    <group>
                        <group string="Student Details">
                            <field name="student_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="hostel_id" readonly="1"/>
                            <field name="room_id" readonly="1"/>
                        </group>
                        <group string="Visit Schedule">
                            <field name="visit_date"/>
                            <field name="visit_time" widget="datetime"/>
                            <field name="exit_time" widget="datetime"/>
                        </group>
                    </group>

                    <group>
                        <group string="Purpose of Visit">
                            <field name="purpose" nolabel="1"
                                   placeholder="Enter the purpose of visit..."/>
                        </group>
                    </group>

                    <group>
                        <group string="Remarks">
                            <field name="remarks" nolabel="1"
                                   placeholder="Enter any additional remarks or notes..."/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View - Hostel Visitor -->
    <record id="hostel_visitor_view_list" model="ir.ui.view">
        <field name="name">hostel.visitor.view.list</field>
        <field name="model">hostel.visitor</field>
        <field name="arch" type="xml">
            <list string="Hostel Visitors"
                  default_order="visit_date desc, visit_time desc"
                  multi_edit="1"
                  sample="1"
                  decoration-success="exit_time != False"
                  decoration-warning="exit_time == False">
                <field name="visit_date"/>
                <field name="visit_time" widget="datetime"/>
                <field name="name"/>
                <field name="visitor_type"/>
                <field name="phone" optional="show"/>
                <field name="student_id"/>
                <field name="hostel_id" optional="show"/>
                <field name="room_id" optional="hide"/>
                <field name="exit_time" widget="datetime" optional="show"/>
                <field name="approved_by" widget="many2one_avatar_user" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Search View - Hostel Visitor -->
    <record id="hostel_visitor_view_search" model="ir.ui.view">
        <field name="name">hostel.visitor.view.search</field>
        <field name="model">hostel.visitor</field>
        <field name="arch" type="xml">
            <search string="Search Visitors">
                <field name="name"/>
                <field name="phone"/>
                <field name="student_id"/>
                <field name="hostel_id"/>
                <field name="room_id"/>
                <field name="visit_date"/>

                <separator/>

                <filter name="filter_today" string="Today's Visits"
                        domain="[('visit_date', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter name="filter_this_week" string="This Week"
                        domain="[('visit_date', '&gt;=', (context_today() - datetime.timedelta(days=context_today().weekday())).strftime('%Y-%m-%d'))]"/>
                <filter name="filter_this_month" string="This Month"
                        domain="[('visit_date', '&gt;=', (context_today().replace(day=1)).strftime('%Y-%m-%d'))]"/>

                <separator/>

                <filter name="filter_checked_in" string="Currently Inside"
                        domain="[('exit_time', '=', False)]"/>
                <filter name="filter_checked_out" string="Checked Out"
                        domain="[('exit_time', '!=', False)]"/>

                <separator/>

                <filter name="filter_parent" string="Parent/Guardian"
                        domain="[('visitor_type', '=', 'parent')]"/>
                <filter name="filter_relative" string="Relative"
                        domain="[('visitor_type', '=', 'relative')]"/>
                <filter name="filter_friend" string="Friend"
                        domain="[('visitor_type', '=', 'friend')]"/>
                <filter name="filter_other" string="Other"
                        domain="[('visitor_type', '=', 'other')]"/>

                <separator/>

                <filter name="filter_approved" string="Approved"
                        domain="[('approved_by', '!=', False)]"/>
                <filter name="filter_pending_approval" string="Pending Approval"
                        domain="[('approved_by', '=', False)]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_visit_date" string="Visit Date"
                            context="{'group_by': 'visit_date'}"/>
                    <filter name="group_by_student" string="Student"
                            context="{'group_by': 'student_id'}"/>
                    <filter name="group_by_hostel" string="Hostel"
                            context="{'group_by': 'hostel_id'}"/>
                    <filter name="group_by_visitor_type" string="Visitor Type"
                            context="{'group_by': 'visitor_type'}"/>
                    <filter name="group_by_approved_by" string="Approved By"
                            context="{'group_by': 'approved_by'}"/>
                </group>

                <searchpanel>
                    <field name="hostel_id" string="Hostel"
                           select="multi"
                           icon="fa-building"/>
                    <field name="visitor_type" string="Visitor Type"
                           select="multi"
                           icon="fa-users"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Hostel Visitor -->
    <record id="hostel_visitor_view_kanban" model="ir.ui.view">
        <field name="name">hostel.visitor.view.kanban</field>
        <field name="model">hostel.visitor</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="visitor_type"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="visitor_type"/>
                <field name="phone"/>
                <field name="id_proof_type"/>
                <field name="id_proof_number"/>
                <field name="student_id"/>
                <field name="hostel_id"/>
                <field name="room_id"/>
                <field name="visit_date"/>
                <field name="visit_time"/>
                <field name="exit_time"/>
                <field name="purpose"/>
                <field name="approved_by"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="ribbon ribbon-top-right" t-if="!record.exit_time.raw_value">
                                <span class="text-bg-warning">Inside</span>
                            </div>
                            <div class="ribbon ribbon-top-right" t-if="record.exit_time.raw_value">
                                <span class="text-bg-success">Checked Out</span>
                            </div>

                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <i class="fa fa-user-circle text-primary"/> <field name="name"/>
                                        </strong>
                                        <div class="text-muted">
                                            <field name="visitor_type"/>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <div class="mb-1">
                                                    <i class="fa fa-phone text-success"/>
                                                    <strong>Phone:</strong> <field name="phone"/>
                                                </div>
                                                <div class="mb-1" t-if="record.id_proof_type.raw_value">
                                                    <i class="fa fa-id-card text-info"/>
                                                    <strong>ID:</strong> <field name="id_proof_type"/> - <field name="id_proof_number"/>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card bg-primary text-white">
                                                <div class="card-body p-2">
                                                    <h6 class="mb-1">
                                                        <i class="fa fa-user-graduate"/> Visiting
                                                    </h6>
                                                    <div><field name="student_id"/></div>
                                                    <small><field name="hostel_id"/> - <field name="room_id"/></small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-6">
                                            <div class="card bg-info text-white">
                                                <div class="card-body p-2 text-center">
                                                    <h6 class="mb-0">
                                                        <i class="fa fa-calendar"/>
                                                    </h6>
                                                    <small><field name="visit_date"/></small>
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-6">
                                            <div class="card bg-warning text-white">
                                                <div class="card-body p-2 text-center">
                                                    <h6 class="mb-0">
                                                        <i class="fa fa-clock-o"/>
                                                    </h6>
                                                    <small><field name="visit_time" widget="datetime"/></small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.exit_time.raw_value">
                                        <div class="col-12">
                                            <div class="card bg-success text-white">
                                                <div class="card-body p-2 text-center">
                                                    <h6 class="mb-0">
                                                        <i class="fa fa-sign-out"/> Check-out
                                                    </h6>
                                                    <small><field name="exit_time" widget="datetime"/></small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.purpose.raw_value">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <strong class="text-muted">
                                                    <i class="fa fa-commenting"/> Purpose:
                                                </strong>
                                                <div><field name="purpose"/></div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.approved_by.raw_value">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <i class="fa fa-check-circle text-success"/>
                                                <strong>Approved by:</strong> <field name="approved_by" widget="many2one_avatar_user"/>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View - Hostel Visitor -->
    <record id="hostel_visitor_view_calendar" model="ir.ui.view">
        <field name="name">hostel.visitor.view.calendar</field>
        <field name="model">hostel.visitor</field>
        <field name="arch" type="xml">
            <calendar string="Visitor Schedule"
                      date_start="visit_time"
                      date_stop="exit_time"
                      mode="month"
                      color="visitor_type"
                      quick_create="0"
                      event_limit="5">
                <field name="name"/>
                <field name="student_id"/>
                <field name="visitor_type"/>
                <field name="hostel_id"/>
            </calendar>
        </field>
    </record>

    <!-- Action -->
    <record id="action_hostel_visitor" model="ir.actions.act_window">
        <field name="name">Visitors</field>
        <field name="res_model">hostel.visitor</field>
        <field name="view_mode">list,kanban,form,calendar</field>
        <field name="context">{'search_default_filter_today': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Hostel Visitor Entry
            </p>
            <p>
                Manage hostel visitor entries and track check-ins/check-outs. Enter name (char "Visitor
                Name", required, e.g., "Ramesh Kumar", "Mrs. Sharma", "Rajesh Patel"). Select visitor_type
                (4 options, required): Parent/Guardian (parents visiting), Relative (family members),
                Friend (student's friends), Other (other visitors). Enter phone (char "Phone Number",
                required, e.g., "+91 9876543210", "9876543210"). Select id_proof_type (4 options):
                Aadhar Card, PAN Card, Driving License, Voter ID. Enter id_proof_number (char, ID proof
                number, e.g., "1234 5678 9012", "ABCDE1234F"). Select student_id (Many2one to
                student.student "Visiting Student", required, indexed, no-create, which student being
                visited). System auto-fills 2 related stored fields: hostel_id (from student_id.hostel_id,
                readonly), room_id (from student_id.room_id, readonly). Enter visit_date (date, default
                today, required). Enter visit_time (datetime "Check-in Time", default now, datetime
                widget). Enter exit_time (datetime "Check-out Time", datetime widget, when visitor leaves).
                Enter purpose (text "Purpose of Visit", why visiting, e.g., "Parent meeting", "Birthday
                celebration", "Academic discussion"). Select approved_by (Many2one to res.users "Approved
                By", avatar widget, warden/admin who approved). Enter remarks (text, additional notes).
                List view shows 2-color row decorations: Green (checked out, exit_time filled), Yellow
                (currently inside, exit_time empty). Kanban view shows dynamic double ribbon: Yellow
                "Inside" (exit_time empty), Green "Checked Out" (exit_time filled). Calendar view displays
                visitor schedule (date_start: visit_time, date_stop: exit_time, color by visitor_type,
                month mode, event_limit 5). Ordered by visit_date desc then visit_time desc (most recent
                first). Today's visits default filter. Full chatter support for communication and tracking.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_hostel_visitor"-->
<!--              name="Visitors"-->
<!--              parent="menu_hostel_root"-->
<!--              action="action_hostel_visitor"-->
<!--              sequence="40"/>-->

</odoo>
