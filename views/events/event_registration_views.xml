<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View - Event Registration -->
    <record id="event_registration_view_form" model="ir.ui.view">
        <field name="name">event.registration.view.form</field>
        <field name="model">event.registration</field>
        <field name="arch" type="xml">
            <form string="Event Registration">
                <header>
                    <button name="action_register" string="Confirm Registration" type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_mark_attended" string="Mark as Attended" type="object"
                            class="btn-success"
                            invisible="state != 'registered'"/>
                    <button name="action_cancel" string="Cancel" type="object"
                            class="btn-danger"
                            invisible="state in ['attended', 'cancelled']"
                            confirm="Are you sure you want to cancel this registration?"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,registered,attended"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Registration"/>
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Event Details">
                            <field name="event_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="registration_date"/>
                        </group>
                        <group string="Participant Details">
                            <field name="participant_type" widget="radio"/>
                            <field name="student_id"
                                   invisible="participant_type != 'student'"
                                   required="participant_type == 'student'"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="faculty_id"
                                   invisible="participant_type != 'faculty'"
                                   required="participant_type == 'faculty'"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="partner_id"
                                   invisible="participant_type != 'external'"
                                   required="participant_type == 'external'"/>
                            <field name="participant_name" readonly="1"
                                   invisible="not participant_name"/>
                            <field name="participant_email" readonly="1"
                                   widget="email"
                                   invisible="not participant_email"/>
                            <field name="participant_phone" readonly="1"
                                   widget="phone"
                                   invisible="not participant_phone"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Fee Payment" name="payment">
                            <group>
                                <group string="Payment Details">
                                    <field name="currency_id" invisible="1"/>
                                    <field name="registration_fee" readonly="1"
                                           widget="monetary"
                                           options="{'currency_field': 'currency_id'}"/>
                                    <field name="fee_paid"/>
                                    <field name="payment_date"
                                           invisible="not fee_paid"
                                           required="fee_paid"/>
                                    <field name="payment_reference"
                                           invisible="not fee_paid"
                                           placeholder="e.g., Transaction ID, Receipt Number"/>
                                </group>
                            </group>
                        </page>

                        <page string="Attendance" name="attendance">
                            <group>
                                <group string="Attendance Information">
                                    <field name="attended"/>
                                    <field name="attendance_time"
                                           readonly="1"
                                           invisible="not attended"/>
                                </group>
                            </group>
                        </page>

                        <page string="Certificate" name="certificate">
                            <group>
                                <group string="Certificate Details">
                                    <field name="certificate_issued"/>
                                    <field name="certificate_number"
                                           invisible="not certificate_issued"
                                           placeholder="e.g., CERT/2025/001"/>
                                    <label for="certificate"
                                           invisible="not certificate_issued"/>
                                    <div class="o_row"
                                         invisible="not certificate_issued">
                                        <field name="certificate"
                                               filename="certificate_filename"
                                               widget="binary"/>
                                    </div>
                                </group>
                            </group>
                        </page>

                        <page string="Remarks" name="remarks">
                            <group>
                                <field name="remarks" nolabel="1"
                                       placeholder="Add any additional notes or comments..."/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View - Event Registration -->
    <record id="event_registration_view_list" model="ir.ui.view">
        <field name="name">event.registration.view.list</field>
        <field name="model">event.registration</field>
        <field name="arch" type="xml">
            <list string="Event Registrations"
                  default_order="registration_date desc"
                  multi_edit="1"
                  sample="1"
                  decoration-info="state == 'draft'"
                  decoration-primary="state == 'registered'"
                  decoration-success="state == 'attended'"
                  decoration-muted="state == 'cancelled'">
                <field name="name"/>
                <field name="event_id"/>
                <field name="participant_type" widget="badge"
                       decoration-info="participant_type == 'student'"
                       decoration-primary="participant_type == 'faculty'"
                       decoration-warning="participant_type == 'external'"
                       optional="show"/>
                <field name="participant_name"/>
                <field name="participant_email" optional="hide"/>
                <field name="participant_phone" optional="hide"/>
                <field name="registration_date"/>
                <field name="currency_id" column_invisible="1"/>
                <field name="registration_fee" optional="show"
                       widget="monetary"/>
                <field name="fee_paid" widget="boolean_toggle" optional="show"/>
                <field name="attended" widget="boolean_toggle" optional="show"/>
                <field name="certificate_issued" widget="boolean_toggle" optional="hide"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-primary="state == 'registered'"
                       decoration-success="state == 'attended'"
                       decoration-danger="state == 'cancelled'"/>
            </list>
        </field>
    </record>

    <!-- Search View - Event Registration -->
    <record id="event_registration_view_search" model="ir.ui.view">
        <field name="name">event.registration.view.search</field>
        <field name="model">event.registration</field>
        <field name="arch" type="xml">
            <search string="Search Registrations">
                <field name="name"/>
                <field name="event_id"/>
                <field name="participant_name"/>
                <field name="participant_email"/>
                <field name="participant_phone"/>

                <separator/>

                <filter name="filter_draft" string="Draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter name="filter_registered" string="Registered"
                        domain="[('state', '=', 'registered')]"/>
                <filter name="filter_attended" string="Attended"
                        domain="[('state', '=', 'attended')]"/>
                <filter name="filter_cancelled" string="Cancelled"
                        domain="[('state', '=', 'cancelled')]"/>

                <separator/>

                <filter name="filter_student" string="Students"
                        domain="[('participant_type', '=', 'student')]"/>
                <filter name="filter_faculty" string="Faculty"
                        domain="[('participant_type', '=', 'faculty')]"/>
                <filter name="filter_external" string="External"
                        domain="[('participant_type', '=', 'external')]"/>

                <separator/>

                <filter name="filter_fee_paid" string="Fee Paid"
                        domain="[('fee_paid', '=', True)]"/>
                <filter name="filter_fee_pending" string="Fee Pending"
                        domain="[('fee_paid', '=', False)]"/>

                <separator/>

                <filter name="filter_attended_yes" string="Attended"
                        domain="[('attended', '=', True)]"/>
                <filter name="filter_attended_no" string="Not Attended"
                        domain="[('attended', '=', False)]"/>

                <separator/>

                <filter name="filter_certificate_issued" string="Certificate Issued"
                        domain="[('certificate_issued', '=', True)]"/>
                <filter name="filter_certificate_pending" string="Certificate Pending"
                        domain="[('certificate_issued', '=', False)]"/>

                <separator/>

                <filter name="filter_this_month" string="This Month"
                        domain="[('registration_date', '&gt;=', (context_today().replace(day=1)).strftime('%Y-%m-%d')),
                                 ('registration_date', '&lt;=', ((context_today().replace(day=1) + datetime.timedelta(days=32)).replace(day=1) - datetime.timedelta(days=1)).strftime('%Y-%m-%d'))]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_state" string="Status"
                            context="{'group_by': 'state'}"/>
                    <filter name="group_by_event" string="Event"
                            context="{'group_by': 'event_id'}"/>
                    <filter name="group_by_participant_type" string="Participant Type"
                            context="{'group_by': 'participant_type'}"/>
                    <filter name="group_by_fee_paid" string="Fee Payment Status"
                            context="{'group_by': 'fee_paid'}"/>
                    <filter name="group_by_registration_date" string="Registration Date"
                            context="{'group_by': 'registration_date'}"/>
                </group>

                <searchpanel>
                    <field name="state" string="Status"
                           icon="fa-flag"/>
                    <field name="event_id" string="Event"
                           select="multi"
                           icon="fa-calendar-check-o"/>
                    <field name="participant_type" string="Participant Type"
                           select="multi"
                           icon="fa-users"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Event Registration -->
    <record id="event_registration_view_kanban" model="ir.ui.view">
        <field name="name">event.registration.view.kanban</field>
        <field name="model">event.registration</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="state"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="event_id"/>
                <field name="participant_type"/>
                <field name="participant_name"/>
                <field name="participant_email"/>
                <field name="participant_phone"/>
                <field name="registration_date"/>
                <field name="registration_fee"/>
                <field name="currency_id"/>
                <field name="fee_paid"/>
                <field name="payment_date"/>
                <field name="attended"/>
                <field name="attendance_time"/>
                <field name="certificate_issued"/>
                <field name="state"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="name"/>
                                        </strong>
                                        <div class="text-muted mt-1">
                                            <small>
                                                <i class="fa fa-calendar"/> <field name="registration_date" widget="datetime"/>
                                            </small>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <h6 class="mb-0">
                                                    <i class="fa fa-calendar-check-o text-primary"/> <field name="event_id"/>
                                                </h6>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card"
                                                 t-attf-class="{{
                                                    record.participant_type.raw_value == 'student' ? 'bg-info' :
                                                    (record.participant_type.raw_value == 'faculty' ? 'bg-primary' : 'bg-warning')
                                                 }} text-white">
                                                <div class="card-body p-2">
                                                    <div class="text-center">
                                                        <h6 class="mb-0">
                                                            <field name="participant_type"/>
                                                        </h6>
                                                        <h5 class="mb-0 mt-1">
                                                            <field name="participant_name"/>
                                                        </h5>
                                                    </div>
                                                    <div class="mt-2" t-if="record.participant_email.raw_value">
                                                        <small>
                                                            <i class="fa fa-envelope"/> <field name="participant_email"/>
                                                        </small>
                                                    </div>
                                                    <div t-if="record.participant_phone.raw_value">
                                                        <small>
                                                            <i class="fa fa-phone"/> <field name="participant_phone"/>
                                                        </small>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.registration_fee.raw_value &gt; 0">
                                        <div class="col-12">
                                            <div class="alert mb-0 p-2"
                                                 t-attf-class="{{
                                                    record.fee_paid.raw_value ? 'alert-success' : 'alert-warning'
                                                 }}">
                                                <div class="text-center">
                                                    <strong>
                                                        <field name="registration_fee" widget="monetary"
                                                               options="{'currency_field': 'currency_id'}"/>
                                                    </strong>
                                                    <small class="d-block">
                                                        <t t-if="record.fee_paid.raw_value">
                                                            <i class="fa fa-check-circle"/> Paid
                                                            <span t-if="record.payment_date.raw_value">
                                                                - <field name="payment_date"/>
                                                            </span>
                                                        </t>
                                                        <t t-else="">
                                                            <i class="fa fa-clock-o"/> Payment Pending
                                                        </t>
                                                    </small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-6" t-if="record.attended.raw_value">
                                            <div class="bg-success text-white p-2 rounded text-center">
                                                <i class="fa fa-check-circle fa-2x"/>
                                                <small class="d-block mt-1">Attended</small>
                                            </div>
                                        </div>
                                        <div class="col-6" t-if="record.certificate_issued.raw_value">
                                            <div class="bg-info text-white p-2 rounded text-center">
                                                <i class="fa fa-certificate fa-2x"/>
                                                <small class="d-block mt-1">Certificate</small>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View - Event Registration -->
    <record id="event_registration_view_calendar" model="ir.ui.view">
        <field name="name">event.registration.view.calendar</field>
        <field name="model">event.registration</field>
        <field name="arch" type="xml">
            <calendar string="Registration Calendar"
                      date_start="registration_date"
                      mode="month"
                      color="participant_type"
                      quick_create="0"
                      event_limit="5">
                <field name="name"/>
                <field name="event_id"/>
                <field name="participant_name"/>
                <field name="participant_type"/>
                <field name="state"/>
            </calendar>
        </field>
    </record>

    <!-- Action -->
    <record id="action_event_registration" model="ir.actions.act_window">
        <field name="name">Registrations</field>
        <field name="res_model">event.registration</field>
        <field name="view_mode">kanban,calendar,list,form</field>
        <field name="context">{'search_default_filter_registered': 1, 'search_default_group_by_event': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Event Registration
            </p>
            <p>
                Manage event registrations with comprehensive participant tracking. Auto-generated unique
                name (Registration Number, readonly sequence). Select event_id (Many2one to university.event
                required tracked indexed no-create). Select participant_type (3 options required, radio
                widget): student (Student - info badge), faculty (Faculty - primary badge), external
                (External Participant - warning/yellow badge). Conditional participant fields (visible and
                required based on participant_type): student_id (Many2one to student.student, no-create),
                faculty_id (Many2one to faculty.faculty, no-create), partner_id (Many2one to res.partner).
                View participant_name (char computed readonly stored via _compute_participant_name with
                api.depends('participant_type','student_id','faculty_id','partner_id') showing selected
                participant's name). View participant_email/participant_phone (char computed readonly via
                _compute_participant_details with api.depends, email/phone widgets). Enter registration_date
                (datetime default now required). View registration_fee (monetary related from
                event_id.registration_fee readonly with monetary widget). Select fee_paid (boolean tracked,
                boolean_toggle widget). Enter payment_date (date, visible/required if fee_paid, in o_row).
                Enter payment_reference (char, visible if fee_paid, placeholder "e.g., Transaction ID,
                Receipt Number"). View currency_id (Many2one to res.currency default company currency,
                invisible/column_invisible). Select attended (boolean tracked, boolean_toggle widget). View
                attendance_time (datetime readonly, visible if attended). Select certificate_issued
                (boolean, boolean_toggle widget). Enter certificate_number (char, visible if
                certificate_issued, placeholder "e.g., CERT/2025/001"). Upload certificate (binary
                attachment, visible if certificate_issued in o_row with label). Enter remarks (text,
                placeholder "Add any additional notes or comments..."). Select state (4 options default
                draft tracked): draft/registered/attended/cancelled. Header has 3 workflow action buttons:
                Confirm Registration (draft→registered via action_register, primary), Mark as Attended
                (registered→attended via action_mark_attended setting attended=True and attendance_time=now,
                success), Cancel (draft/registered→cancelled via action_cancel with confirmation dialog,
                danger). Statusbar widget for state (visible draft,registered,attended). Form view with
                notebook having 4 pages: Fee Payment (registration_fee/fee_paid/payment_date/
                payment_reference), Attendance (attended/attendance_time), Certificate (certificate_issued/
                certificate_number/certificate upload), Remarks (remarks field). List view with 4-color row
                decorations: Info (draft), Primary/Blue (registered), Success/Green (attended), Muted/Gray
                (cancelled). Badge widgets for participant_type (3-color decorations info/primary/warning)
                and state (4-color decorations info/primary/success/danger). Boolean_toggle widgets for
                fee_paid/attended/certificate_issued. Monetary widget for registration_fee. Optional field
                controls. Kanban view grouped by state showing name as title, calendar icon with
                registration_date datetime widget small text, event_id card (light background,
                calendar-check-o icon primary/blue), participant_type card (dynamic 3-color background:
                info/student, primary/faculty, warning/external, centered, showing participant_type/
                participant_name h5, participant_email with envelope icon, participant_phone with phone
                icon), conditional registration_fee alert (dynamic 2-color: success if fee_paid, warning if
                not, centered, showing monetary field with "Paid - payment_date" or "Payment Pending" text,
                shown if registration_fee &gt; 0), 2-card grid (6/6) showing check-circle icon 2x success
                "Attended" if attended=True, certificate icon 2x info "Certificate" if
                certificate_issued=True. Calendar view for timeline (date_start: registration_date, color
                by participant_type, month mode). Ordered by registration_date desc (most recent first).
                Default filters: Registered status and grouped by Event. Comprehensive search filters for 4
                states/3 participant types/fee paid-pending/attended yes-no/certificate issued-pending/this
                month/group by state-event-participant_type-fee_paid-registration_date. Searchpanel for
                state/event_id/participant_type multi-select. Validation constraint _check_max_participants
                checking event_id.max_participants limit raising ValidationError if exceeded. Full chatter/
                activity/portal support. SQL constraint for unique name. Complete event registration
                lifecycle management with multi-participant type support.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_event_registration"-->
<!--              name="Registrations"-->
<!--              parent="menu_events_root"-->
<!--              action="action_event_registration"-->
<!--              sequence="20"/>-->

</odoo>
