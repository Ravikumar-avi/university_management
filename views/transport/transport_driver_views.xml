<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View - Transport Driver -->
    <record id="transport_driver_view_form" model="ir.ui.view">
        <field name="name">transport.driver.view.form</field>
        <field name="model">transport.driver</field>
        <field name="arch" type="xml">
            <form string="Transport Driver">
                <sheet>
                    <field name="photo" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <label for="name" string="Driver Name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Rajesh Kumar"/>
                        </h1>
                        <field name="driver_code" readonly="1"/>
                    </div>

                    <group>
                        <group string="Personal Information">
                            <field name="partner_id" invisible="1"/>
                            <field name="mobile" widget="phone"/>
                            <field name="email" widget="email"/>
                            <field name="joining_date"/>
                            <field name="experience_years"/>
                        </group>
                        <group string="Employment Status">
                            <field name="state" widget="badge"
                                   decoration-success="state == 'active'"
                                   decoration-warning="state == 'on_leave'"
                                   decoration-danger="state in ['suspended', 'resigned']"/>
                            <field name="active" widget="boolean_toggle"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="License Details" name="license">
                            <group>
                                <group string="License Information">
                                    <field name="license_number"/>
                                    <field name="license_type" widget="radio"/>
                                </group>
                                <group string="License Validity">
                                    <field name="license_issue_date"/>
                                    <field name="license_expiry_date"
                                           decoration-danger="license_expiry_date and license_expiry_date &lt; context_today()"
                                           decoration-warning="license_expiry_date and license_expiry_date &lt; (context_today() + datetime.timedelta(days=30))"/>
                                </group>
                            </group>
                        </page>

                        <page string="Vehicle Assignment" name="vehicles">
                            <group>
                                <field name="current_vehicle_id"
                                       options="{'no_create': True, 'no_create_edit': True}"/>
                            </group>
                            <field name="vehicle_ids" nolabel="1">
                                <list string="Assigned Vehicles" editable="bottom">
                                    <field name="name"/>
                                    <field name="vehicle_number"/>
                                    <field name="vehicle_type"/>
                                    <field name="seating_capacity"/>
                                </list>
                            </field>
                        </page>

                        <page string="Contact Details" name="contact">
                            <group>
                                <group string="Address">
                                    <field name="street" placeholder="Street..."/>
                                    <field name="street2" placeholder="Street 2..."/>
                                    <field name="city" placeholder="City"/>
                                    <field name="state_id" options="{'no_create': True}"/>
                                    <field name="zip" placeholder="PIN Code"/>
                                    <field name="country_id" options="{'no_create': True}"/>
                                </group>
                                <group string="Emergency Contact">
                                    <field name="emergency_contact_name"
                                           placeholder="Emergency Contact Person"/>
                                    <field name="emergency_contact_phone"
                                           widget="phone"
                                           placeholder="Emergency Phone Number"/>
                                </group>
                            </group>
                        </page>

                        <page string="Government IDs" name="govt_ids">
                            <group>
                                <group string="Identity Documents">
                                    <field name="aadhar_number"
                                           placeholder="XXXX-XXXX-XXXX"/>
                                    <field name="pan_number"
                                           placeholder="ABCDE1234F"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View - Transport Driver -->
    <record id="transport_driver_view_list" model="ir.ui.view">
        <field name="name">transport.driver.view.list</field>
        <field name="model">transport.driver</field>
        <field name="arch" type="xml">
            <list string="Transport Drivers"
                  default_order="name"
                  multi_edit="1"
                  sample="1"
                  decoration-success="state == 'active'"
                  decoration-warning="state == 'on_leave'"
                  decoration-muted="state == 'resigned'"
                  decoration-danger="state == 'suspended'">
                <field name="driver_code"/>
                <field name="name"/>
                <field name="mobile" optional="show"/>
                <field name="email" optional="show"/>
                <field name="license_number"/>
                <field name="license_type" optional="show" widget="badge"/>
                <field name="license_expiry_date" optional="show"
                       decoration-danger="license_expiry_date and license_expiry_date &lt; context_today()"
                       decoration-warning="license_expiry_date and license_expiry_date &lt; (context_today() + datetime.timedelta(days=30))"/>
                <field name="current_vehicle_id" optional="show"/>
                <field name="experience_years" optional="hide"/>
                <field name="joining_date" optional="hide"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'active'"
                       decoration-warning="state == 'on_leave'"
                       decoration-danger="state in ['suspended', 'resigned']"/>
                <field name="active" widget="boolean_toggle" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Search View - Transport Driver -->
    <record id="transport_driver_view_search" model="ir.ui.view">
        <field name="name">transport.driver.view.search</field>
        <field name="model">transport.driver</field>
        <field name="arch" type="xml">
            <search string="Search Drivers">
                <field name="name"/>
                <field name="driver_code"/>
                <field name="mobile"/>
                <field name="email"/>
                <field name="license_number"/>
                <field name="current_vehicle_id"/>

                <separator/>

                <filter name="filter_active" string="Active"
                        domain="[('state', '=', 'active')]"/>
                <filter name="filter_on_leave" string="On Leave"
                        domain="[('state', '=', 'on_leave')]"/>
                <filter name="filter_suspended" string="Suspended"
                        domain="[('state', '=', 'suspended')]"/>
                <filter name="filter_resigned" string="Resigned"
                        domain="[('state', '=', 'resigned')]"/>

                <separator/>

                <filter name="filter_license_expiring" string="License Expiring Soon"
                        domain="[('license_expiry_date', '&lt;', (context_today() + datetime.timedelta(days=30)).strftime('%Y-%m-%d')),
                                 ('license_expiry_date', '&gt;=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter name="filter_license_expired" string="License Expired"
                        domain="[('license_expiry_date', '&lt;', context_today().strftime('%Y-%m-%d'))]"/>

                <separator/>

                <filter name="filter_light_license" string="Light Vehicle License"
                        domain="[('license_type', '=', 'light')]"/>
                <filter name="filter_heavy_license" string="Heavy Vehicle License"
                        domain="[('license_type', '=', 'heavy')]"/>
                <filter name="filter_transport_license" string="Transport Vehicle License"
                        domain="[('license_type', '=', 'transport')]"/>

                <separator/>

                <filter name="filter_assigned_vehicle" string="Has Current Vehicle"
                        domain="[('current_vehicle_id', '!=', False)]"/>
                <filter name="filter_no_vehicle" string="No Current Vehicle"
                        domain="[('current_vehicle_id', '=', False)]"/>

                <separator/>

                <filter name="filter_inactive" string="Archived"
                        domain="[('active', '=', False)]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_state" string="Status"
                            context="{'group_by': 'state'}"/>
                    <filter name="group_by_license_type" string="License Type"
                            context="{'group_by': 'license_type'}"/>
                    <filter name="group_by_current_vehicle" string="Current Vehicle"
                            context="{'group_by': 'current_vehicle_id'}"/>
                    <filter name="group_by_joining_date" string="Joining Month"
                            context="{'group_by': 'joining_date'}"/>
                </group>

                <searchpanel>
                    <field name="state" string="Status"
                           select="multi"
                           icon="fa-flag"/>
                    <field name="license_type" string="License Type"
                           select="multi"
                           icon="fa-id-card"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Transport Driver -->
    <record id="transport_driver_view_kanban" model="ir.ui.view">
        <field name="name">transport.driver.view.kanban</field>
        <field name="model">transport.driver</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="state"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="driver_code"/>
                <field name="photo"/>
                <field name="mobile"/>
                <field name="email"/>
                <field name="license_number"/>
                <field name="license_type"/>
                <field name="license_expiry_date"/>
                <field name="current_vehicle_id"/>
                <field name="experience_years"/>
                <field name="state"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="ribbon ribbon-top-right" t-if="record.state.raw_value == 'active'">
                                <span class="text-bg-success">Active</span>
                            </div>
                            <div class="ribbon ribbon-top-right" t-if="record.state.raw_value == 'on_leave'">
                                <span class="text-bg-warning">On Leave</span>
                            </div>
                            <div class="ribbon ribbon-top-right" t-if="record.state.raw_value == 'suspended'">
                                <span class="text-bg-danger">Suspended</span>
                            </div>
                            <div class="ribbon ribbon-top-right" t-if="record.state.raw_value == 'resigned'">
                                <span class="text-bg-secondary">Resigned</span>
                            </div>

                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <div class="row">
                                            <div class="col-3">
                                                <field name="photo" widget="image"
                                                       options="{'size': [80, 80]}"
                                                       class="oe_avatar"/>
                                            </div>
                                            <div class="col-9">
                                                <strong class="o_kanban_record_title">
                                                    <field name="name"/>
                                                </strong>
                                                <div class="text-muted">
                                                    <i class="fa fa-id-badge"/> <field name="driver_code"/>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card bg-light">
                                                <div class="card-body p-2">
                                                    <div t-if="record.mobile.raw_value">
                                                        <strong class="text-primary">
                                                            <i class="fa fa-phone"/> Mobile:
                                                        </strong>
                                                        <span><field name="mobile"/></span>
                                                    </div>
                                                    <div t-if="record.email.raw_value">
                                                        <strong class="text-success">
                                                            <i class="fa fa-envelope"/> Email:
                                                        </strong>
                                                        <span><field name="email"/></span>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card bg-info text-white">
                                                <div class="card-body p-2">
                                                    <h6 class="mb-1">
                                                        <i class="fa fa-id-card"/> License Details
                                                    </h6>
                                                    <div>
                                                        <strong>No:</strong> <field name="license_number"/>
                                                    </div>
                                                    <div>
                                                        <strong>Type:</strong> <field name="license_type"/>
                                                    </div>
                                                    <div t-if="record.license_expiry_date.raw_value">
                                                        <strong>Expires:</strong> <field name="license_expiry_date"/>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.current_vehicle_id.raw_value">
                                        <div class="col-12">
                                            <div class="card bg-primary text-white">
                                                <div class="card-body p-2 text-center">
                                                    <h6 class="mb-0">
                                                        <i class="fa fa-bus"/> Current Vehicle
                                                    </h6>
                                                    <div><field name="current_vehicle_id"/></div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.experience_years.raw_value">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded text-center">
                                                <strong class="text-warning">
                                                    <i class="fa fa-briefcase"/> Experience:
                                                </strong>
                                                <span><field name="experience_years"/> years</span>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View - Transport Driver -->
    <record id="transport_driver_view_calendar" model="ir.ui.view">
        <field name="name">transport.driver.view.calendar</field>
        <field name="model">transport.driver</field>
        <field name="arch" type="xml">
            <calendar string="Driver License Expiry"
                      date_start="license_expiry_date"
                      mode="month"
                      color="state"
                      quick_create="0"
                      event_limit="5">
                <field name="name"/>
                <field name="license_number"/>
                <field name="state"/>
            </calendar>
        </field>
    </record>

    <!-- Pivot View - Transport Driver -->
    <record id="transport_driver_view_pivot" model="ir.ui.view">
        <field name="name">transport.driver.view.pivot</field>
        <field name="model">transport.driver</field>
        <field name="arch" type="xml">
            <pivot string="Driver Analysis">
                <field name="state" type="row"/>
                <field name="license_type" type="col"/>
                <field name="experience_years" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- Graph View - Transport Driver -->
    <record id="transport_driver_view_graph" model="ir.ui.view">
        <field name="name">transport.driver.view.graph</field>
        <field name="model">transport.driver</field>
        <field name="arch" type="xml">
            <graph string="Driver Statistics" type="bar">
                <field name="license_type"/>
                <field name="state" type="col"/>
            </graph>
        </field>
    </record>

    <!-- Action -->
    <record id="action_transport_driver" model="ir.actions.act_window">
        <field name="name">Transport Drivers</field>
        <field name="res_model">transport.driver</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph</field>
        <field name="context">{'search_default_filter_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add Transport Driver
            </p>
            <p>
                Manage transport drivers with complete profiles. System auto-generates driver_code
                (from sequence, unique, readonly). Model _inherits res.partner (contact integration):
                Inherits name (driver name), mobile (phone widget), email (email widget), address fields
                (street, street2, city, state_id, zip, country_id). Upload photo (binary, attachment,
                avatar widget). Enter license_number (required, tracked, unique constraint). Select
                license_type (3 options, required, radio widget): light (Light Motor Vehicle - cars/jeeps),
                heavy (Heavy Vehicle - trucks), transport (Transport Vehicle - buses). Enter
                license_issue_date. Enter license_expiry_date (tracked, danger decoration if expired,
                warning decoration if expiring within 30 days). Enter experience_years (integer). Assign
                vehicle_ids (One2many to transport.vehicle "Vehicles Assigned", editable list). Select
                current_vehicle_id (Many2one to transport.vehicle, no-create). Enter emergency_contact_name
                and emergency_contact_phone (phone widget). Enter aadhar_number (XXXX-XXXX-XXXX format) and
                pan_number (ABCDE1234F format) government IDs. Enter joining_date. Select state (4 options,
                default active, tracked, badge widget): active (Active - working), on_leave (On Leave -
                temporary absence), suspended (Suspended - disciplinary), resigned (Resigned - left). Toggle
                active boolean (default true, boolean_toggle widget). List view shows 4-color row decorations:
                Green (active), Yellow (on_leave), Gray (resigned), Red (suspended). Badge widgets for
                license_type and state. License expiry date with danger/warning decorations. Kanban view
                shows dynamic quadruple ribbon (Active green, On Leave yellow, Suspended red, Resigned gray)
                with avatar photo, contact card, license card (info/blue), current vehicle card (primary/blue),
                and experience card. Calendar view displays license expiry dates (date_start:
                license_expiry_date, color by state, month mode, event_limit 5). Pivot view for analysis
                (state × license_type matrix, measure: experience_years). Graph view for statistics (bar
                chart, license_type vs. state). Ordered by name (alphabetical). Default filter: Active
                drivers. Full chatter support for communication.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_transport_driver"-->
<!--              name="Drivers"-->
<!--              parent="menu_transport_root"-->
<!--              action="action_transport_driver"-->
<!--              sequence="20"/>-->

</odoo>
