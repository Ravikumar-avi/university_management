<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View - Transport Allocation -->
    <record id="transport_allocation_view_form" model="ir.ui.view">
        <field name="name">transport.allocation.view.form</field>
        <field name="model">transport.allocation</field>
        <field name="arch" type="xml">
            <form string="Transport Allocation">
                <header>
                    <button name="action_activate" string="Activate" type="object"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>
                    <button name="action_cancel" string="Cancel" type="object"
                            invisible="state == 'cancelled'"
                            confirm="Are you sure you want to cancel this allocation?"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active,cancelled"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Allocation Number"/>
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Student Information">
                            <field name="student_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="registration_number"/>
                            <field name="academic_year_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                        </group>
                        <group string="Transport Details">
                            <field name="route_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="stop_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="vehicle_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                        </group>
                    </group>

                    <group>
                        <group string="Allocation Period">
                            <field name="allocation_date"/>
                            <field name="from_date"/>
                            <field name="to_date"/>
                        </group>
                        <group string="Fee Details">
                            <field name="monthly_fee" widget="monetary"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>

                    <group string="Remarks">
                        <field name="remarks" nolabel="1"
                               placeholder="Enter any special instructions, pickup points, or notes..."/>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View - Transport Allocation -->
    <record id="transport_allocation_view_list" model="ir.ui.view">
        <field name="name">transport.allocation.view.list</field>
        <field name="model">transport.allocation</field>
        <field name="arch" type="xml">
            <list string="Transport Allocations"
                  default_order="allocation_date desc"
                  multi_edit="1"
                  sample="1"
                  decoration-success="state == 'active'"
                  decoration-muted="state == 'draft'"
                  decoration-danger="state == 'cancelled'">
                <field name="name"/>
                <field name="allocation_date"/>
                <field name="student_id"/>
                <field name="registration_number" optional="show"/>
                <field name="route_id"/>
                <field name="stop_id" optional="show"/>
                <field name="vehicle_id" optional="hide"/>
                <field name="from_date" optional="show"/>
                <field name="to_date" optional="show"/>
                <field name="monthly_fee" optional="show" widget="monetary"/>
                <field name="academic_year_id" optional="hide"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'active'"
                       decoration-muted="state == 'draft'"
                       decoration-danger="state == 'cancelled'"/>
                <field name="currency_id" column_invisible="1"/>
            </list>
        </field>
    </record>

    <!-- Search View - Transport Allocation -->
    <record id="transport_allocation_view_search" model="ir.ui.view">
        <field name="name">transport.allocation.view.search</field>
        <field name="model">transport.allocation</field>
        <field name="arch" type="xml">
            <search string="Search Transport Allocations">
                <field name="name"/>
                <field name="student_id"/>
                <field name="registration_number"/>
                <field name="route_id"/>
                <field name="stop_id"/>
                <field name="vehicle_id"/>

                <separator/>

                <filter name="filter_draft" string="Draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter name="filter_active" string="Active"
                        domain="[('state', '=', 'active')]"/>
                <filter name="filter_cancelled" string="Cancelled"
                        domain="[('state', '=', 'cancelled')]"/>

                <separator/>

                <filter name="filter_current_allocations" string="Current Allocations"
                        domain="[('from_date', '&lt;=', context_today().strftime('%Y-%m-%d')),
                                 '|', ('to_date', '&gt;=', context_today().strftime('%Y-%m-%d')),
                                 ('to_date', '=', False)]"/>
                <filter name="filter_future_allocations" string="Future Allocations"
                        domain="[('from_date', '&gt;', context_today().strftime('%Y-%m-%d'))]"/>
                <filter name="filter_expired_allocations" string="Expired Allocations"
                        domain="[('to_date', '&lt;', context_today().strftime('%Y-%m-%d'))]"/>

                <separator/>

                <filter name="filter_this_month" string="This Month"
                        domain="[('allocation_date', '&gt;=', (context_today().replace(day=1)).strftime('%Y-%m-%d'))]"/>
                <filter name="filter_this_year" string="This Year"
                        domain="[('allocation_date', '&gt;=', (context_today().replace(month=1, day=1)).strftime('%Y-%m-%d'))]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_student" string="Student"
                            context="{'group_by': 'student_id'}"/>
                    <filter name="group_by_route" string="Route"
                            context="{'group_by': 'route_id'}"/>
                    <filter name="group_by_stop" string="Boarding Point"
                            context="{'group_by': 'stop_id'}"/>
                    <filter name="group_by_vehicle" string="Vehicle"
                            context="{'group_by': 'vehicle_id'}"/>
                    <filter name="group_by_state" string="Status"
                            context="{'group_by': 'state'}"/>
                    <filter name="group_by_academic_year" string="Academic Year"
                            context="{'group_by': 'academic_year_id'}"/>
                </group>

                <searchpanel>
                    <field name="route_id" string="Route"
                           select="multi"
                           icon="fa-bus"/>
                    <field name="state" string="Status"
                           select="multi"
                           icon="fa-flag"/>
                    <field name="academic_year_id" string="Academic Year"
                           select="multi"
                           icon="fa-calendar-alt"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Transport Allocation -->
    <record id="transport_allocation_view_kanban" model="ir.ui.view">
        <field name="name">transport.allocation.view.kanban</field>
        <field name="model">transport.allocation</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="state"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="student_id"/>
                <field name="registration_number"/>
                <field name="route_id"/>
                <field name="stop_id"/>
                <field name="vehicle_id"/>
                <field name="allocation_date"/>
                <field name="from_date"/>
                <field name="to_date"/>
                <field name="monthly_fee"/>
                <field name="state"/>
                <field name="remarks"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="ribbon ribbon-top-right" t-if="record.state.raw_value == 'active'">
                                <span class="text-bg-success">Active</span>
                            </div>
                            <div class="ribbon ribbon-top-right" t-if="record.state.raw_value == 'draft'">
                                <span class="text-bg-secondary">Draft</span>
                            </div>
                            <div class="ribbon ribbon-top-right" t-if="record.state.raw_value == 'cancelled'">
                                <span class="text-bg-danger">Cancelled</span>
                            </div>

                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <field name="name"/>
                                        </strong>
                                        <div class="text-muted">
                                            <i class="fa fa-calendar"/> <field name="allocation_date"/>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card bg-primary text-white">
                                                <div class="card-body p-2">
                                                    <div class="d-flex align-items-center">
                                                        <i class="fa fa-user fa-2x me-2"/>
                                                        <div>
                                                            <h6 class="mb-0"><field name="student_id"/></h6>
                                                            <small><field name="registration_number"/></small>
                                                        </div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card bg-light">
                                                <div class="card-body p-2">
                                                    <strong class="text-primary">
                                                        <i class="fa fa-bus"/> Route:
                                                    </strong>
                                                    <div><field name="route_id"/></div>
                                                    <strong class="text-success mt-1 d-block">
                                                        <i class="fa fa-map-marker-alt"/> Boarding Point:
                                                    </strong>
                                                    <div><field name="stop_id"/></div>
                                                    <div t-if="record.vehicle_id.raw_value">
                                                        <strong class="text-info mt-1 d-block">
                                                            <i class="fa fa-car"/> Vehicle:
                                                        </strong>
                                                        <div><field name="vehicle_id"/></div>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-6">
                                            <div class="bg-light p-2 rounded text-center">
                                                <small class="text-muted">From</small>
                                                <div><strong><field name="from_date"/></strong></div>
                                            </div>
                                        </div>
                                        <div class="col-6">
                                            <div class="bg-light p-2 rounded text-center">
                                                <small class="text-muted">To</small>
                                                <div><strong><field name="to_date"/></strong></div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.monthly_fee.raw_value">
                                        <div class="col-12">
                                            <div class="card bg-warning text-white">
                                                <div class="card-body p-2 text-center">
                                                    <h5 class="mb-0">
                                                        <i class="fa fa-rupee-sign"/> <field name="monthly_fee"/>
                                                    </h5>
                                                    <small>Monthly Fee</small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.remarks.raw_value">
                                        <div class="col-12">
                                            <div class="bg-light p-2 rounded">
                                                <strong class="text-muted">
                                                    <i class="fa fa-sticky-note"/> Remarks:
                                                </strong>
                                                <div><field name="remarks"/></div>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View - Transport Allocation -->
    <record id="transport_allocation_view_calendar" model="ir.ui.view">
        <field name="name">transport.allocation.view.calendar</field>
        <field name="model">transport.allocation</field>
        <field name="arch" type="xml">
            <calendar string="Transport Allocation Calendar"
                      date_start="from_date"
                      date_stop="to_date"
                      mode="month"
                      color="state"
                      quick_create="0"
                      event_limit="5">
                <field name="student_id"/>
                <field name="route_id"/>
                <field name="state"/>
            </calendar>
        </field>
    </record>

    <!-- Pivot View - Transport Allocation -->
    <record id="transport_allocation_view_pivot" model="ir.ui.view">
        <field name="name">transport.allocation.view.pivot</field>
        <field name="model">transport.allocation</field>
        <field name="arch" type="xml">
            <pivot string="Transport Allocation Analysis">
                <field name="route_id" type="row"/>
                <field name="state" type="col"/>
                <field name="monthly_fee" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- Graph View - Transport Allocation -->
    <record id="transport_allocation_view_graph" model="ir.ui.view">
        <field name="name">transport.allocation.view.graph</field>
        <field name="model">transport.allocation</field>
        <field name="arch" type="xml">
            <graph string="Transport Allocation Statistics" type="bar">
                <field name="route_id"/>
                <field name="state" type="col"/>
            </graph>
        </field>
    </record>

    <!-- Action -->
    <record id="action_transport_allocation" model="ir.actions.act_window">
        <field name="name">Transport Allocation</field>
        <field name="res_model">transport.allocation</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph</field>
        <field name="context">{'search_default_filter_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Allocate Transport to Students
            </p>
            <p>
                Manage student transport/bus allocations. System auto-generates name (allocation number
                from sequence, unique, readonly). Select student_id (Many2one to student.student, required,
                tracked, indexed, no-create). Shows registration_number (related, readonly). Select route_id
                (Many2one to transport.route, required, tracked, indexed, no-create). Select stop_id
                (Many2one to transport.stop "Boarding Point", required, tracked, no-create, domain filtered
                by route_id). Select vehicle_id (Many2one to transport.vehicle, optional, no-create, domain
                filtered by route_id). Enter allocation_date (date, default today, required, tracked). Enter
                from_date (date "From Date", required, validity start). Enter to_date (date "To Date",
                optional, validity end). Select academic_year_id (Many2one to university.academic.year,
                optional, no-create). Enter monthly_fee (monetary, auto-filled from route_id on change).
                Select state (3 options, default draft, tracked, statusbar): draft (Draft - new allocation),
                active (Active - confirmed allocation), cancelled (Cancelled - terminated). Enter remarks
                (text, special instructions). List view shows 3-color row decorations: Green (active), Gray
                (draft), Red (cancelled). Badge widget for state with 3 decoration colors. Kanban view shows
                dynamic triple ribbon (Active green, Draft gray, Cancelled red) with student card, route/stop
                details, period dates, and fee card. Calendar view displays allocations by period (date_start:
                from_date, date_stop: to_date, color by state, month mode, event_limit 5). Pivot view for
                analysis (route × state matrix, measure: monthly_fee sum). Graph view for statistics (bar
                chart, route vs. state). Ordered by allocation_date desc (most recent first). Default filter:
                Active allocations. Header buttons: Activate (from draft), Cancel (confirm). Full chatter
                support for communication and tracking.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_transport_allocation"-->
<!--              name="Student Allocation"-->
<!--              parent="menu_transport_root"-->
<!--              action="action_transport_allocation"-->
<!--              sequence="40"/>-->

</odoo>
