<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Form View - Placement Offer -->
    <record id="placement_offer_view_form" model="ir.ui.view">
        <field name="name">placement.offer.view.form</field>
        <field name="model">placement.offer</field>
        <field name="arch" type="xml">
            <form string="Placement Offer">
                <header>
                    <button name="action_send_offer" string="Send Offer" type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_accept" string="Accept Offer" type="object"
                            class="btn-success"
                            invisible="state != 'offered'"/>
                    <button name="action_reject" string="Reject Offer" type="object"
                            class="btn-danger"
                            invisible="state != 'offered'"/>
                    <button name="action_withdraw" string="Withdraw Offer" type="object"
                            class="btn-warning"
                            invisible="state in ['accepted', 'rejected', 'withdrawn']"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,offered,accepted"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <label for="name" string="Offer Letter Number"/>
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Student Details">
                            <field name="student_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="registration_number"/>
                        </group>
                        <group string="Placement Drive">
                            <field name="drive_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="company_id"/>
                            <field name="application_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                        </group>
                    </group>

                    <group>
                        <group string="Offer Details">
                            <field name="offer_date"/>
                            <field name="job_title" placeholder="e.g. Software Engineer"/>
                            <field name="job_location" placeholder="e.g. Bangalore"/>
                        </group>
                        <group string="Acceptance Timeline">
                            <field name="acceptance_deadline"/>
                            <field name="acceptance_date" readonly="1"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Salary Package" name="salary">
                            <group>
                                <group string="Compensation (Per Annum)">
                                    <field name="ctc" widget="monetary"/>
                                    <field name="fixed_component" widget="monetary"/>
                                    <field name="variable_component" widget="monetary"/>
                                    <field name="currency_id" invisible="1"/>
                                </group>
                            </group>
                        </page>

                        <page string="Joining Details" name="joining">
                            <group>
                                <group string="Joining Information">
                                    <field name="joining_date"/>
                                    <field name="joining_location" placeholder="e.g. Bangalore Office"/>
                                </group>
                            </group>
                        </page>

                        <page string="Bond Details" name="bond">
                            <group>
                                <group string="Service Bond">
                                    <field name="has_bond"/>
                                    <field name="bond_duration"
                                           invisible="not has_bond"
                                           placeholder="Enter duration in months"/>
                                    <field name="bond_amount" widget="monetary"
                                           invisible="not has_bond"/>
                                </group>
                            </group>
                        </page>

                        <page string="Offer Letter" name="letter">
                            <group>
                                <group string="Offer Letter Document">
                                    <field name="offer_letter" filename="offer_letter_filename"/>
                                    <field name="offer_letter_filename" invisible="1"/>
                                </group>
                            </group>
                        </page>

                        <page string="Remarks" name="remarks">
                            <group>
                                <field name="rejection_reason"
                                       invisible="state != 'rejected'"
                                       placeholder="Enter reason for rejection..."/>
                                <field name="remarks" placeholder="Additional remarks or notes..."/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- List View - Placement Offer -->
    <record id="placement_offer_view_list" model="ir.ui.view">
        <field name="name">placement.offer.view.list</field>
        <field name="model">placement.offer</field>
        <field name="arch" type="xml">
            <list string="Placement Offers"
                  default_order="offer_date desc"
                  multi_edit="1"
                  sample="1"
                  decoration-info="state == 'draft'"
                  decoration-primary="state == 'offered'"
                  decoration-success="state == 'accepted'"
                  decoration-danger="state == 'rejected'"
                  decoration-muted="state == 'withdrawn'">
                <field name="name"/>
                <field name="student_id"/>
                <field name="registration_number" optional="hide"/>
                <field name="drive_id" optional="show"/>
                <field name="company_id"/>
                <field name="job_title"/>
                <field name="job_location" optional="hide"/>
                <field name="ctc" optional="show" widget="monetary"/>
                <field name="offer_date"/>
                <field name="joining_date" optional="show"/>
                <field name="acceptance_deadline" optional="hide"/>
                <field name="acceptance_date" optional="hide"/>
                <field name="has_bond" optional="hide" widget="boolean_toggle"/>
                <field name="state" widget="badge"
                       decoration-info="state == 'draft'"
                       decoration-primary="state == 'offered'"
                       decoration-success="state == 'accepted'"
                       decoration-danger="state == 'rejected'"
                       decoration-muted="state == 'withdrawn'"/>
            </list>
        </field>
    </record>

    <!-- Search View - Placement Offer -->
    <record id="placement_offer_view_search" model="ir.ui.view">
        <field name="name">placement.offer.view.search</field>
        <field name="model">placement.offer</field>
        <field name="arch" type="xml">
            <search string="Search Placement Offers">
                <field name="name"/>
                <field name="student_id"/>
                <field name="registration_number"/>
                <field name="drive_id"/>
                <field name="company_id"/>
                <field name="job_title"/>

                <separator/>

                <filter name="filter_draft" string="Draft"
                        domain="[('state', '=', 'draft')]"/>
                <filter name="filter_offered" string="Offered"
                        domain="[('state', '=', 'offered')]"/>
                <filter name="filter_accepted" string="Accepted"
                        domain="[('state', '=', 'accepted')]"/>
                <filter name="filter_rejected" string="Rejected"
                        domain="[('state', '=', 'rejected')]"/>
                <filter name="filter_withdrawn" string="Withdrawn"
                        domain="[('state', '=', 'withdrawn')]"/>

                <separator/>

                <filter name="filter_with_bond" string="With Service Bond"
                        domain="[('has_bond', '=', True)]"/>
                <filter name="filter_without_bond" string="Without Service Bond"
                        domain="[('has_bond', '=', False)]"/>

                <separator/>

                <filter name="filter_this_month" string="This Month"
                        domain="[('offer_date', '&gt;=', (context_today().replace(day=1)).strftime('%Y-%m-%d')),
                                 ('offer_date', '&lt;=', ((context_today().replace(day=1) + datetime.timedelta(days=32)).replace(day=1) - datetime.timedelta(days=1)).strftime('%Y-%m-%d'))]"/>
                <filter name="filter_this_year" string="This Year"
                        domain="[('offer_date', '&gt;=', (context_today().replace(month=1, day=1)).strftime('%Y-%m-%d'))]"/>

                <separator/>

                <filter name="filter_deadline_approaching" string="Deadline Approaching"
                        domain="[('acceptance_deadline', '&lt;=', (context_today() + datetime.timedelta(days=7)).strftime('%Y-%m-%d')),
                                 ('acceptance_deadline', '&gt;=', context_today().strftime('%Y-%m-%d')),
                                 ('state', '=', 'offered')]"/>
                <filter name="filter_deadline_passed" string="Deadline Passed"
                        domain="[('acceptance_deadline', '&lt;', context_today().strftime('%Y-%m-%d')),
                                 ('state', '=', 'offered')]"/>

                <group expand="0" string="Group By">
                    <filter name="group_by_student" string="Student"
                            context="{'group_by': 'student_id'}"/>
                    <filter name="group_by_company" string="Company"
                            context="{'group_by': 'company_id'}"/>
                    <filter name="group_by_drive" string="Placement Drive"
                            context="{'group_by': 'drive_id'}"/>
                    <filter name="group_by_state" string="Status"
                            context="{'group_by': 'state'}"/>
                    <filter name="group_by_offer_date" string="Offer Date"
                            context="{'group_by': 'offer_date'}"/>
                </group>

                <searchpanel>
                    <field name="company_id" string="Company"
                           select="multi"
                           icon="fa-building"/>
                    <field name="state" string="Status"
                           select="multi"
                           icon="fa-flag"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <!-- Kanban View - Placement Offer -->
    <record id="placement_offer_view_kanban" model="ir.ui.view">
        <field name="name">placement.offer.view.kanban</field>
        <field name="model">placement.offer</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile"
                    default_group_by="state"
                    quick_create="false"
                    sample="1">
                <field name="id"/>
                <field name="name"/>
                <field name="student_id"/>
                <field name="registration_number"/>
                <field name="drive_id"/>
                <field name="company_id"/>
                <field name="job_title"/>
                <field name="job_location"/>
                <field name="ctc"/>
                <field name="offer_date"/>
                <field name="joining_date"/>
                <field name="acceptance_deadline"/>
                <field name="acceptance_date"/>
                <field name="has_bond"/>
                <field name="bond_duration"/>
                <field name="state"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong class="o_kanban_record_title">
                                            <i class="fa fa-file-text-o"/> <field name="name"/>
                                        </strong>
                                        <div class="text-muted">
                                            <i class="fa fa-user"/> <field name="student_id"/>
                                        </div>
                                        <div class="text-muted" t-if="record.registration_number.raw_value">
                                            <small><field name="registration_number"/></small>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_body mt-3">
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card bg-primary text-white">
                                                <div class="card-body p-2">
                                                    <h6 class="mb-1">
                                                        <i class="fa fa-building"/> <field name="company_id"/>
                                                    </h6>
                                                    <small t-if="record.drive_id.raw_value">
                                                        <i class="fa fa-calendar-check"/> <field name="drive_id"/>
                                                    </small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <div class="card bg-success text-white">
                                                <div class="card-body p-2">
                                                    <h6 class="mb-0">
                                                        <i class="fa fa-briefcase"/> <field name="job_title"/>
                                                    </h6>
                                                    <small t-if="record.job_location.raw_value">
                                                        <i class="fa fa-map-marker"/> <field name="job_location"/>
                                                    </small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.ctc.raw_value">
                                        <div class="col-12">
                                            <div class="card bg-warning text-white">
                                                <div class="card-body p-2 text-center">
                                                    <h6 class="mb-0">
                                                        <i class="fa fa-money"/> CTC: <field name="ctc"/> PA
                                                    </h6>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2">
                                        <div class="col-6">
                                            <div class="bg-light p-2 rounded text-center">
                                                <div class="text-info">
                                                    <i class="fa fa-calendar fa-lg"/>
                                                </div>
                                                <small class="text-muted">Offer Date</small>
                                                <h6 class="mb-0"><field name="offer_date"/></h6>
                                            </div>
                                        </div>
                                        <div class="col-6" t-if="record.joining_date.raw_value">
                                            <div class="bg-light p-2 rounded text-center">
                                                <div class="text-success">
                                                    <i class="fa fa-sign-in fa-lg"/>
                                                </div>
                                                <small class="text-muted">Joining Date</small>
                                                <h6 class="mb-0"><field name="joining_date"/></h6>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.acceptance_deadline.raw_value or record.acceptance_date.raw_value">
                                        <div class="col-12">
                                            <div class="card bg-info text-white">
                                                <div class="card-body p-2">
                                                    <small t-if="record.acceptance_deadline.raw_value">
                                                        <i class="fa fa-clock-o"/> Deadline: <field name="acceptance_deadline"/>
                                                    </small>
                                                    <span t-if="record.acceptance_deadline.raw_value and record.acceptance_date.raw_value"> | </span>
                                                    <small t-if="record.acceptance_date.raw_value">
                                                        <i class="fa fa-check"/> Accepted: <field name="acceptance_date"/>
                                                    </small>
                                                </div>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="row mb-2" t-if="record.has_bond.raw_value">
                                        <div class="col-12">
                                            <div class="alert alert-danger mb-0 p-2">
                                                <small>
                                                    <i class="fa fa-link"/> Service Bond: <field name="bond_duration"/> Months
                                                </small>
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_right">
                                        <field name="activity_ids" widget="kanban_activity"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View - Placement Offer -->
    <record id="placement_offer_view_calendar" model="ir.ui.view">
        <field name="name">placement.offer.view.calendar</field>
        <field name="model">placement.offer</field>
        <field name="arch" type="xml">
            <calendar string="Placement Offers Calendar"
                      date_start="offer_date"
                      mode="month"
                      color="company_id"
                      quick_create="0"
                      event_limit="5">
                <field name="name"/>
                <field name="student_id"/>
                <field name="company_id"/>
                <field name="job_title"/>
                <field name="ctc"/>
                <field name="state"/>
            </calendar>
        </field>
    </record>

    <!-- Pivot View - Placement Offer -->
    <record id="placement_offer_view_pivot" model="ir.ui.view">
        <field name="name">placement.offer.view.pivot</field>
        <field name="model">placement.offer</field>
        <field name="arch" type="xml">
            <pivot string="Offer Analysis">
                <field name="company_id" type="row"/>
                <field name="state" type="col"/>
                <field name="ctc" type="measure"/>
            </pivot>
        </field>
    </record>

    <!-- Graph View - Placement Offer -->
    <record id="placement_offer_view_graph" model="ir.ui.view">
        <field name="name">placement.offer.view.graph</field>
        <field name="model">placement.offer</field>
        <field name="arch" type="xml">
            <graph string="Offer Statistics" type="bar">
                <field name="company_id"/>
                <field name="ctc" type="measure"/>
            </graph>
        </field>
    </record>

    <!-- Action -->
    <record id="action_placement_offer" model="ir.actions.act_window">
        <field name="name">Placement Offers</field>
        <field name="res_model">placement.offer</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph</field>
        <field name="context">{'search_default_filter_offered': 1, 'search_default_group_by_state': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create Placement Offer
            </p>
            <p>
                Manage placement offer letters. System auto-generates name (offer letter number from
                sequence, unique, readonly). Select student_id (Many2one to student.student, required,
                tracked, indexed, no-create). Shows registration_number (related from student_id.
                registration_number, readonly). Select drive_id (Many2one to placement.drive, required,
                tracked, indexed, no-create). Shows company_id (Many2one related from drive_id.company_id,
                stored, readonly). Select application_id (Many2one to placement.application, optional,
                no-create). Enter offer details: offer_date (date, default today, required, tracked),
                job_title (char, required, placeholder "Software Engineer"), job_location (char,
                placeholder "Bangalore"). Enter acceptance timeline: acceptance_deadline (date),
                acceptance_date (date, readonly, auto-filled on accept action). Enter salary package
                (per annum): ctc (monetary, required, monetary widget), fixed_component (monetary,
                monetary widget), variable_component (monetary, monetary widget), currency_id (Many2one
                to res.currency, default company currency, invisible). Enter joining details:
                joining_date (date, tracked), joining_location (char, placeholder "Bangalore Office").
                Enter bond details: has_bond (boolean), bond_duration (integer months, invisible if not
                has_bond, placeholder "Enter duration in months"), bond_amount (monetary, monetary
                widget, invisible if not has_bond). Upload offer_letter (binary, attachment true,
                filename field offer_letter_filename). Select state (5 options, default draft, tracked,
                statusbar widget): draft (Draft), offered (Offered), accepted (Accepted), rejected
                (Rejected), withdrawn (Withdrawn). Enter rejection_reason (text, invisible if not
                rejected state, placeholder). Enter remarks (text, placeholder). Header has 4 action
                buttons with visibility rules: Send Offer (draft→offered, sends email via template
                university_management.email_template_placement_offer, primary button), Accept Offer
                (offered→accepted, auto-fills acceptance_date with today, success button), Reject Offer
                (offered→rejected, danger button), Withdraw Offer (any→withdrawn except
                accepted/rejected/withdrawn, warning button). Unique name SQL constraint. List view with
                5-color row decorations: Info/Blue (draft), Primary (offered), Success/Green (accepted),
                Danger/Red (rejected), Muted (withdrawn). Badge widget for state with 5 decoration
                colors. Boolean toggle for has_bond. Kanban view grouped by state showing student card
                (user icon), company card (primary/blue, building icon, shows drive_id), job card
                (success/green, briefcase icon, job_title/job_location), salary card (warning/yellow,
                money icon, ctc, conditional), dates cards (2 cards 6/6 grid: offer_date info,
                joining_date success conditional), acceptance card (info, clock/check icons,
                acceptance_deadline/acceptance_date, conditional), and bond alert (danger, link icon,
                bond_duration, conditional if has_bond). Calendar view for offer tracking (date_start:
                offer_date, color by company_id, month mode, event_limit 5). Pivot view for analysis
                (company × state matrix, measure: ctc). Graph view for statistics (bar chart, company
                vs. ctc). Ordered by offer_date desc (most recent first). Default filters: Offered
                status and Grouped by Status. Full chatter and portal support for student access.
            </p>
        </field>
    </record>

    <!-- Menu Item -->
<!--    <menuitem id="menu_placement_offer"-->
<!--              name="Placement Offers"-->
<!--              parent="menu_placement_root"-->
<!--              action="action_placement_offer"-->
<!--              sequence="25"/>-->

</odoo>
